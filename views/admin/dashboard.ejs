<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= pageTitle %>
  </title>

  <style>
    /* RESET */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
    }

    /* HOME CARD */
    .home-card {
      background: #eef4ff;
      border: 1px solid #d8e4ff;
    }

    .home-card .icon {
      color: #2b5fd9;
    }

    .home-card h3 {
      color: #1f3f8a;
    }

    .home-card span {
      color: #3b5ca8;
    }

    .home-card:hover {
      box-shadow: 0 10px 26px rgba(43, 95, 217, 0.25);
    }


    body {
      background: #f5f7f8;
      color: #2c2c2c;
      min-height: 100vh;
    }

    /* CONTAINER */
    .admin-wrapper {
      max-width: 1100px;
      margin: auto;
      padding: 1.5rem 1rem 3rem;
    }

    /* HEADING */
    .admin-title {
      text-align: center;
      margin-bottom: 5rem;
    }

    .admin-title h1 {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 0.3rem;
    }

    .admin-title p {
      font-size: 0.95rem;
      color: #666;
    }

    /* NAV GRID */
    .admin-nav {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
    }

    /* CARD */
    .admin-card {
      background: #ffffff;
      border-radius: 16px;
      padding: 1.5rem 1rem;
      text-align: center;
      text-decoration: none;
      color: inherit;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
      transition: transform 0.15s ease, box-shadow 0.15s ease;
    }

    .admin-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 26px rgba(0, 0, 0, 0.12);
    }

    .admin-card .icon {
      font-size: 2rem;
      margin-bottom: 0.6rem;
    }

    .admin-card h3 {
      font-size: 1rem;
      font-weight: 600;
      margin-bottom: 0.3rem;
    }

    .admin-card span {
      font-size: 0.85rem;
      color: #777;
    }

    /* LOGOUT CARD */
    .logout-card {
      padding: 0;
    }

    .logout-inner {
      width: 100%;
      height: 100%;
      background: transparent;
      border: none;
      cursor: pointer;
      color: inherit;
      padding: 1.5rem 1rem;
      border-radius: 16px;
    }

    .logout-card {
      background: #fff5f5;
      border: 1px solid #ffd6d6;
    }

    .logout-card .icon {
      color: #d64545;
    }

    .logout-card h3 {
      color: #b82c2c;
    }

    .logout-card span {
      color: #8f3a3a;
    }

    .logout-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 26px rgba(214, 69, 69, 0.25);
    }


    /* DESKTOP */
    @media (min-width: 768px) {
      .admin-title h1 {
        font-size: 2.2rem;
      }

      .admin-nav {
        grid-template-columns: repeat(4, 1fr);
        gap: 1.4rem;
      }

      .admin-card {
        padding: 2rem 1.2rem;
      }

      .admin-card .icon {
        font-size: 2.4rem;
      }
    }
  </style>
</head>

<body>


  <div class="admin-wrapper">

    <div class="admin-title">
      <h1>Admin Panel</h1>
      <p>Manage your store from one place</p>
    </div>

    <nav class="admin-nav">

      <!-- HOME -->
      <a href="/" class="admin-card home-card">
        <div class="icon">🏠</div>
        <h3>Home</h3>
        <span>View website</span>
      </a>

      <a href="/admin/products" class="admin-card">
        <div class="icon">📦</div>
        <h3>Products</h3>
        <span>Manage items</span>
      </a>

      <a href="/admin/orders" class="admin-card">
        <div class="icon">🧾</div>
        <h3>Orders</h3>
        <span>View purchases</span>
      </a>

      <a href="/admin/users" class="admin-card">
        <div class="icon">👤</div>
        <h3>Users</h3>
        <span>Customer list</span>
      </a>

      <a href="/admin/stats" class="admin-card">
        <div class="icon">📊</div>
        <h3>Stats</h3>
        <span>Store analytics</span>
      </a>

      <!-- LOGOUT CARD -->
      <form action="/admin/logout" method="POST" class="admin-card logout-card">
        <button type="submit" class="logout-inner">
          <div class="icon">🚪</div>
          <h3>Logout</h3>
          <span>Exit admin panel</span>
        </button>
      </form>
    </nav>

  </div>
</body>

</html>